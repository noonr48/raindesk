[critical] FREEFORM_DESK_V2.md §1; public/js/creative-desk.js worldToScreen / screenToWorld / renderObject / installDrag; public/js/app.js freeform mount; public/js/window-manager.js renderFrame / installDrag / installResize / persist; lib/workspace.js SCREEN_TYPES -> Mechanism: The documented unified world-space model is still split between two coordinate authorities. CreativeDesk objects are transformed through the persisted pan/zoom viewport and convert pointer deltas into world units, while all seven registry windows are classified as screen-space and store raw stage pixels. Panning or zooming the creative world therefore moves the artwork but not its associated creative surfaces; their persisted positions have incompatible semantics, and the manager cannot form spatial groups with world objects. This remains an architectural blocker for the endless creative-desk contract. -> Recommendation: Make world coordinates the canonical persisted geometry for creative surfaces, with an explicit projection layer for rendering and pointer input. Treat docking as a temporary screen-space presentation of a world-owned window, or formally separate true application chrome from creative windows and prohibit mixed-space grouping. -> Acceptance test: Place a window beside a known artwork feature, record its canonical rect, then pan, zoom, resize the viewport, reload, and restore from a dock. The window must retain the same world relationship to the artwork without rewriting its canonical rect; explicitly declared screen-only chrome must remain viewport-fixed.

[major] public/js/window-manager.js open / persist / close; public/js/api.js deleteWorkspaceWindow; server-core.js /api/workspace/object and /api/workspace/window/delete; lib/workspace.js upsertWindow / deleteWindow -> Mechanism: Close is still not authoritative. open() immediately queues an asynchronous upsert, but close() queues deletion only when model.persisted is already true. Closing before the first response skips deletion, after which the pending upsert creates the server row and the window resurrects on reload. The same failure occurs when the server committed an upsert but its response was lost. Successful delete responses return res.workspace.revision, while close() reads res.revision, leaving its token stale. Delete conflicts are only warned about—there is no adoption and retry—and any stale external client can recreate the deleted ID because ungated upsert creates missing rows. -> Recommendation: Introduce server-owned lifecycle identity: an incarnation/generation or tombstone per windowId, revision-gated create/update, and idempotent delete. Always enqueue close after pending writes regardless of model.persisted; adopt workspace.revision, retry a 409 against current state, and treat an already-absent row as successful deletion. -> Acceptance test: Delay the initial upsert, call open() then close() immediately, release the upsert, and verify that GET contains no row. Repeat with a lost upsert response, a delete 409, an idempotent repeated delete, and a stale client update issued after deletion; none may recreate the window.

[major] lib/workspace.js write / assertBaseRevision / upsertWindow / setViewport / setGroups / setShelf; public/js/window-manager.js writeChain / persistStructure / persistShelfMembership; public/js/creative-desk.js persistObject / persistViewport; public/js/workspace-ui.js persist -> Mechanism: Structural concurrency uses one workspace-wide revision that is also incremented by unrelated last-write-wins geometry and viewport writes. The manager’s comment that writeChain serializes “ALL workspace writes” is false at application scope: CreativeDesk and WorkspaceShell have separate queues, and a pan, sheet movement, or legacy-panel update can invalidate a freeform group, shelf, or delete request from the same page. On 409, groups and shelf retry the same whole local collection once rather than merging server state; a disjoint edit from another tab or Partner can therefore be overwritten. A prior transport warning also sets the same flag used as the retry latch, suppressing a later conflict retry. -> Recommendation: Separate structural revisioning from spatial/viewport revisioning and replace whole-collection replacement with intent operations such as group.addMember, group.removeMember, shelf.minimise, and shelf.restore. Give retries an independent bounded counter and merge or reject against the affected entity rather than replaying a stale collection. -> Acceptance test: Continuously pan CreativeDesk and move a legacy panel while two clients perform disjoint group and shelf operations from the same starting revision. All structural intents must survive, unrelated spatial writes must not conflict, and an earlier network failure must not disable the next legitimate retry.

[major] public/js/window-manager.js minimise / restore / removeFromGroup / persist; lib/workspace.js setShelf / setGroups / validateWorkspace; lib/tests/workspace-routes.test.js group-and-shelf case -> Mechanism: Minimising a grouped window is not one structural transition. The client removes the member locally, but minimise() never calls persistStructure(), and ordinary persist() omits groupId. The subsequent shelf request changes only shelf membership and window state; the server deliberately leaves the minimised window inside its group. A reload can therefore restore a shelf member that is also a tab-group member, with client and server disagreeing about ownership. The route test currently institutionalizes this invalid hybrid by shelving a grouped member without asserting group removal. -> Recommendation: Implement one revision-gated server operation that atomically removes the member from its group, selects or dissolves the remaining group, sets the window state, and updates shelf membership. Return the canonical workspace and replace the corresponding client structures from it. -> Acceptance test: Minimise the active and inactive members of two-, three-, and singleton-policy groups. After every operation and reload, each minimised window must be absent from all groups, present exactly once on the shelf, and the surviving group must have a valid active member.

[major] public/js/window-manager.js groupWindows / joinGroup / removeFromGroup / init; lib/workspace.js setGroups / validateWorkspace / deleteWindow -> Mechanism: The group graph admits contradictory membership and state. groupWindows() neither deduplicates its input nor removes members from prior groups. setGroups() allows one window to appear in multiple group arrays and silently assigns its window.groupId to the last group, because validation does not enforce the reverse relation. When joinGroup() creates a group around an ungrouped target, it transitions the target to tabbed only locally and persists only the joining member; the target can reload as floating inside a group. The server preserves singleton groups after member removal, while init() discards groups with fewer than two members, producing a recurring client/server mismatch. -> Recommendation: Move group construction and membership changes into canonical server transactions. Enforce unique membership across the complete group collection, exact bidirectional group.windowIds ↔ window.groupId correspondence, a single documented singleton policy, and state compatibility for every member. -> Acceptance test: Property-test random join, regroup, close, minimise, tear-out, and reload sequences. After each operation, every window must belong to at most one group, every reverse reference must match, group arrays must contain no duplicates, active members must exist, and client/server snapshots must be identical.

[major] public/js/window-manager.js renderFrame / transition / groupWindows / switchTab / applySnap / maximise -> Mechanism: A group is still a list of independent windows rather than one spatial frame. Each member retains its own rect, z-order, dock, restore rect, and lifecycle state, so switching tabs can make the frame jump to another geometry. renderFrame() hides an inactive member only while its state remains tabbed; docking or maximising the active member changes that member’s state, after which switching tabs can leave both the former and new active members visible. Per-window gesture locking also does not protect this shared group aggregate: simultaneous gestures on different member IDs may mutate the same group. -> Recommendation: Give each group a first-class frame record containing rect, z-order, dock, maximise/restore state, and active member. Members should own content identity only. Route drag, resize, dock, maximise, focus, and gesture locking through the group frame whenever a window is grouped. -> Acceptance test: Create a group from windows with different geometries, repeatedly switch tabs, resize, dock, maximise, restore, and reload. The frame must never move merely because the active tab changes, exactly one member’s content must be visible, and concurrent member-pointer gestures must be serialized at group scope.

[major] public/js/window-manager.js installResize / renderFrame / init; public/js/workspace-ui.js dockRect; public/js/app.js boot resize handler -> Mechanism: Docking is not maintained as a geometric invariant. A left-docked window can be resized from its west edge, and a right-docked window from its east edge, moving the supposedly anchored edge while state === 'docked' and dock remain unchanged. No manager or ResizeObserver reflows docked windows when stage dimensions change, so a right rail detaches after viewport shrink or growth and jumps back only after reload. dockRect() also preserves arbitrary prior width and height, allowing an extremely wide floating window to become an obstructive “rail.” -> Recommendation: Define dock geometry as edge + constrained cross-axis size + along-edge extent, not an arbitrary floating rect. Either preserve the anchored edge during resize or explicitly undock when its outer handle is pulled. Reflow docked windows and normalize floating windows whenever stage bounds change, with per-surface rail-size constraints. -> Acceptance test: Exercise all eight resize handles on left- and right-docked instances, then resize and rotate the viewport through narrow and wide dimensions. The edge must remain anchored or the window must visibly become floating; geometry before and after reload must be equivalent, and docked widths must remain within the surface’s declared limits.

[major] public/js/window-manager.js minimise / restore / transition / init; public/js/freeform-surfaces.js Takes registration; lib/workspace.js setShelf and dock sanitization -> Mechanism: Dock state is not durable through shelf or policy transitions. A docked model retains model.dock when minimised, but restore() always transitions it to floating; the subsequent upsert stores state: 'floating' with a non-null dock, and shelf removal does not convert it back to docked. On initialization, an out-of-policy persisted dock—such as Takes, which opts out—changes only the local state to floating; the stale dock value is neither cleared nor persisted, so the same malformed row returns every session. The server validates only the four edge names and has no per-surface policy or state/dock consistency rule. -> Recommendation: Persist an explicit pre-minimise restore state or make shelf restore an atomic server transition. Enforce docked ⇔ allowed non-null dock and clear disallowed docks using a single shared surface-policy definition consumed by client and server. Persist initialization repairs immediately and idempotently. -> Acceptance test: Dock each eligible surface left and right, minimise, restore, and reload; it must return to the same edge. Seed Takes and another restricted surface with state: 'docked' and every forbidden edge; one initialization must permanently rewrite each row to state: 'floating', dock: null.

[major] public/js/window-manager.js beginGesture / installDrag / installResize / installTabTear / installShelfChipDrag / abortGesturesFor -> Mechanism: The kernel closes same-window drag and resize races, but tab-tear and shelf-chip sessions still lack a severed-buttons terminal and do not capture the pointer. If release occurs outside the browser while the document remains visible, the session and document listeners can survive; mouse pointer IDs are commonly reused, so a later interaction can satisfy the stale pointerup and tear or restore at unrelated coordinates. Resize also calls setPointerCapture() before confirming that beginGesture() acquired the lock, leaving a rejected second pointer captured without an owning session. Given the recorded false positives, adding raw blur or unqualified lostpointercapture globally would be unsafe; the missing protection is site-specific terminal handling. -> Recommendation: Add strict buttons === 0 cancellation and session-generation checks to tab and shelf gestures, acquire the kernel lock before pointer capture, and ensure every rejected or terminated path releases capture and listeners. Use filtered capture-loss handling only where a site can prove ownership. -> Acceptance test: For drag, all eight resize directions, tab tear, and shelf drag, inject foreign pointers, pointercancel, hidden visibility, close, Escape where supported, buttons === 0, release outside the viewport, and pointer-ID reuse. Every session must terminate exactly once, cancelled geometry must roll back, and the immediately following gesture must start cleanly.

[major] public/js/app.js init, legacy BeatTrail construction, WorkspaceShell registrations, and mountBeatTrail -> Mechanism: Freeform mode remains additive rather than ownership-transferring. The legacy BeatTrail is constructed before the freeform gate; opening the registry Beats surface creates a second BeatTrail and overwrites state.beatTrail. Closing that surface sets state.beatTrail = null instead of restoring or retaining an owner, so later shot switches and Partner-turn refreshes no longer update any trail through the shared seam. Legacy Layers and Scenes are also always registered with WorkspaceShell while registry-owned equivalents can be open simultaneously. These legacy writers additionally operate outside the freeform manager’s write chain. -> Recommendation: Resolve the mode before constructing controllers. Introduce an ownership table with exactly one live controller per logical surface, explicit transfer/destroy hooks during migration, and one stable façade for shared app references such as the active BeatTrail. Do not initialize legacy WorkspaceShell owners when the registry owns those surfaces. -> Acceptance test: In freeform mode, assert one controller, one visible frame, and one listener set for Layers, Scenes, and Beats. Open, close, and reopen Beats, switch shots, and trigger Partner refreshes; the single current trail must continue receiving every update, with no legacy workspace writes.

[major] public/js/window-manager.js ensureFrame / focus / renderGroupTabs; public/css/freeform-desk.css resize handles, tabs, and shelf -> Mechanism: Core window operations remain pointer-only and the DOM does not expose a complete accessible window model. Window sections have no role or accessible name, logical focus() never moves DOM focus, grouped buttons lack tablist / tab / aria-selected semantics, and resize handles are aria-hidden with 9–15 px hit regions. There is no keyboard path for moving, resizing, docking, grouping, or tearing out a window, and focus restoration after minimise, close, or tab switch is undefined. -> Recommendation: Add named window/dialog regions, managed focus, semantic tablists, announced state changes, and keyboard commands for move, resize, dock, minimise, maximise, close, and tab navigation. Provide larger invisible hit regions for pointer resizing without changing the visual chrome. -> Acceptance test: Complete the normal open, move, resize, group, switch, minimise, restore, dock, maximise, and close journey using only a keyboard, then repeat with a screen reader. Focus must remain visible and deterministic, and every state transition must expose the correct accessible name, role, selection, and status.

[major] public/js/app.js freeform query gate; public/js/window-manager.js clampRect / defaultRect / init; public/css/freeform-desk.css; dev/browser-freeform-desk-journey.js fixed viewport launch -> Mechanism: The freeform manager mounts at any viewport size when ?freeform=1, but minimum-size handling can deliberately create a window wider or taller than the stage. Initialization clamps dimensions but not persisted x/y through normalizedRect, so an old large-screen placement can restore with its entire title bar off-screen. Existing windows are not reclaimed when the viewport shrinks, and there is no keyboard or command-based recovery path. The only native journey runs at a fixed 1440×900 size, so this failure class is unobserved. -> Recommendation: Either gate the feature to a documented desktop range or implement responsive lifecycle rules: normalize restored geometry, reflow on stage resize, preserve a reachable title-bar strip, respect safe areas and shelf/chrome reservations, and expose “bring all windows into view.” -> Acceptance test: Seed extreme and negative persisted coordinates, then test first load, live resize, and orientation changes at 320×568, 768×1024, 1280×720, 1440×900, and an ultrawide viewport. Every open window must retain reachable controls and be recoverable without clearing storage.

[major] dev/browser-freeform-desk-journey.js reload/geometry and Notes steps; tests/frontend/freeform-window-manager.test.js close, dock, and gesture cases; lib/tests/workspace-routes.test.js shelf/group case -> Mechanism: The current evidence suite cannot falsify several round-4 regressions. The journey labels geometry restoration without recording and comparing the rect, and its reload sequence explicitly opens Notes, masking whether a closed Notes row resurrected. The close unit test waits for the initial persistence tick before closing, excluding the actual race. Gesture locking is principally exercised as drag-versus-drag, not across drag/resize/tab/shelf and their interruption terminals. Dock tests omit docked resize, viewport reflow, dock→shelf→restore, and persistent policy repair. The route test accepts a minimised member remaining grouped. -> Recommendation: Replace source-shape and existence checks with state-transition assertions against both manager state and GET /api/workspace; add deterministic deferred promises for each wire race and mutation tests for the critical guards. Run the browser journey across multiple viewport sizes and at least the supported browser engines. -> Acceptance test: The suite must fail when independently mutating away: unconditional close deletion, delete revision adoption, group removal on minimise, dock restore, dock reflow, tab/shelf severed cancellation, root/world coordinate conversion, and pre/post-reload rect equality.

[minor] public/js/window-manager.js snapPlace and drag modifier handling; public/js/workspace-ui.js edgeSnap; public/css/freeform-desk.css .freeform-snap-zone -> Mechanism: Visible drop targets and actual activation geometry disagree. Left and right zones cover 32% of the desk, while docking activates only when the window edge is within the 18 px snap threshold; releasing across most of the visible wash does nothing. Preview uses the last pointer-move event’s altKey, while commit recomputes from pointer-up, so changing Alt without another move can make the displayed result differ from settlement. -> Recommendation: Make the visible zone itself the hit-test contract, or reduce it to the true edge band, and update preview state on modifier-key changes. Settle from the same computed candidate that was last presented. -> Acceptance test: Sample releases across every pixel band of the visible zone and assert that visual active/inactive state exactly predicts settlement; press and release Alt without moving and verify preview and commit remain identical.

[minor] public/js/window-manager.js activeGestures, showSnapPreview, showSnapZones, clearSnapPreview, and clearSnapZones -> Mechanism: Gesture ownership is per window, so two different windows may be dragged concurrently, but snap preview and zone state are manager-wide singletons. Moves from either pointer overwrite the same overlay, and termination of one gesture clears feedback still owned by the other. The underlying windows may commit correctly while the artist receives incorrect or disappearing placement feedback. -> Recommendation: Key preview and zone ownership by gesture session, or explicitly enforce one desk-level docking drag while allowing other non-docking gestures. A terminal may clear only overlays owned by its session. -> Acceptance test: Drag two different docking-capable windows with two pointers, move them toward opposite edges, and end one first. Each supported simultaneous gesture must retain accurate feedback until its own terminal, or the second drag must be rejected visibly and consistently.

[minor] public/js/window-manager.js CreativeSurfaces.register / CreativeSurfaces.get -> Mechanism: Surface definitions are described as frozen, but supportedStates is a mutable Set returned through the public registry. Object.freeze() freezes the containing object, not the Set’s contents, so any controller or integration code can add docked to Takes or remove required states at runtime, bypassing the declared policy. -> Recommendation: Store states as a frozen array or private immutable set and expose only supportsState(state) / supportsDock(edge) predicates. Do not return mutable policy objects from the registry. -> Acceptance test: Attempt to mutate every retrieved surface policy through add, delete, array modification, and retained references; all attempts must throw or have no effect, and Takes must remain non-dockable.

TOP-3 next work items

Replace the workspace mutation protocol with identity-safe, intent-based operations — very high impact / medium effort.
Acceptance criterion: Immediate close, lost responses, stale clients, simultaneous tabs, Partner writes, continuous viewport movement, grouping, and shelving complete without resurrection, unrelated 409s, or lost disjoint edits; every returned workspace satisfies lifecycle, group, shelf, and dock invariants.

Introduce one authoritative spatial/container model for world placement, groups, and docks — highest architectural impact / high effort.
Acceptance criterion: World-relative windows preserve their relationship to artwork through pan, zoom, viewport resize, docking, grouping, tab switching, maximise/restore, and reload; a group has one frame, and every dock remains anchored or explicitly transitions to floating.

Close the gesture kernel with adversarial browser evidence — high impact / low-to-medium effort.
Acceptance criterion: A four-site-by-terminal matrix covers drag, eight-way resize, tab tear, and shelf drag against commit, cancel, severed buttons, hidden visibility, close, foreign pointers, outside release, and pointer-ID reuse; mutation testing proves that removing any required terminal or ownership guard causes a deterministic failure.
