[critical] FREEFORM_DESK_V2.md §§1–3; public/js/creative-desk.js worldToScreen / screenToWorld / renderObject / installDrag; public/js/window-manager.js clampRect / installDrag / installResize / persist / init; lib/workspace.js SCREEN_TYPES -> Mechanism: The promised unified world-space model is still split between two coordinate authorities. CreativeDesk projects canonical world units through the persisted pan/zoom viewport, while every shipped registry surface type is classified as screen and WindowManager stores raw stage pixels derived directly from clientX/clientY. Panning or zooming therefore moves artwork and creative sheets but not Scenes, Layers, Beats, Takes, Characters, Notes, or Proposals; their persisted coordinates have incompatible meanings, and mixed grouping cannot be spatially coherent. -> Recommendation: Make world coordinates the canonical persisted geometry for creative surfaces and introduce one projection/input layer for rendering and pointer deltas. Reserve screen space only for explicitly declared application chrome; docking should be temporary screen presentation with a world-owned restore rect. -> Acceptance test: Place each creative surface beside a known artwork feature, then pan, zoom, resize the stage, reload, dock, minimise, and restore it. Its canonical world rect must remain unchanged and its relationship to the artwork must remain constant; explicitly screen-only chrome must remain viewport-fixed.

[major] public/js/window-manager.js close catch chain; public/js/api.js ApiError / deleteWorkspaceWindow; server-core.js /api/workspace/window/delete -> Mechanism: The first delete catch treats every error without an attached workspace as successful absence. A legitimate 404 has no workspace, but neither do network failures, authentication failures, ordinary 4xx responses, or 5xx responses. Those failures resolve to undefined, bypass the final warning catch, and leave the window removed locally while its server row survives to resurrect on reload. Conversely, a 404 from the one conflict retry reaches the final catch and is warned as failure. -> Recommendation: Dispatch explicitly by status: 404 is idempotent success; 409 with a workspace is conflict handling; every other error must remain rejected and enter a durable failed-close state with retry/reconciliation. Do not infer “absent” merely from the lack of error.workspace. -> Acceptance test: Exercise first-attempt and retry-path 404s, status 0 transport loss, 401, 500, connection reset after request transmission, and malformed responses. Only confirmed 404 may settle as success; every uncertain failure must remain visible/retriable, and a subsequent GET must determine whether the row still exists.

[major] public/js/window-manager.js close; lib/workspace.js upsertWindow / deleteWindow / assertBaseRevision -> Mechanism: Unconditional enqueueing closes the immediate-open race, but deletion is still not lifecycle-authoritative. A second external revision change can invalidate the single retry, after which close is abandoned. More fundamentally, object upserts remain ungated and create a missing windowId, so any stale tab or Partner writer can recreate an already-deleted identity after a fully successful close. There is no incarnation, tombstone, or generation distinguishing the deleted instance from a later intentional reopening. -> Recommendation: Give every window lifecycle identity: an incarnation/generation or durable tombstone. Gate create/update by that identity, make delete idempotent against it, and reconcile conflicts until reaching a terminal “this incarnation is absent” result rather than retrying once. -> Acceptance test: Delay the initial upsert, close immediately, lose the upsert response, force multiple consecutive delete conflicts, repeat delete, and then release a stale update from another client. The closed incarnation must never reappear; reopening must create a distinguishable new incarnation.

[major] public/js/window-manager.js transition / minimise / restore; public/css/freeform-desk.css .freeform-window-collapsed; tests/frontend/freeform-window-manager.test.js “minimise then restore returns a docked window…” -> Mechanism: The dock-through-shelf repair restores the lifecycle state but not visible content. transition(..., 'minimised') sets collapsed = true; transition(..., 'docked') does not clear it, because only floating and maximised clear collapse. A docked window restored from the shelf is therefore visible as a header-only rail, persists as {state:'docked', collapsed:true}, and remains collapsed after reload. The new test seeds exactly that state but asserts only state and x. -> Recommendation: Define a canonical restore-visible transition that normalizes all coupled fields. At minimum, shelf restoration to floating, docked, or active tabbed must clear minimisation-derived collapse; if user-controlled collapse remains orthogonal, distinguish it from shelf collapse explicitly. -> Acceptance test: Perform a real dock → minimise → shelf-click restore cycle and restore a seeded minimised+docked row. Assert collapsed === false, body content is rendered, the persisted row is normalized, and the same result survives reload.

[major] public/js/window-manager.js installResize anchored-edge undock and cancel -> Mechanism: Pulling an anchored resize handle mutates dock = null and transitions the model from docked to floating during pointermove. Cancellation restores only current.rect; it does not restore state, dock, collapsed, or restoreRect. A pointercancel, hidden-tab terminal, or buttons === 0 therefore leaves a floating model with the old docked rectangle while the server still contains the docked row. The next reload visibly changes state again. -> Recommendation: Snapshot the complete lifecycle transaction before resize—at least {rect, state, dock, collapsed, restoreRect}—and restore it atomically on every cancel terminal. Better, stage undocking as gesture-local state and commit it only on successful release. -> Acceptance test: For left, right, top, and bottom docks, pull every handle containing the anchored direction and interrupt with pointercancel, buttons === 0, and document.hidden. Every field must equal the pre-gesture snapshot, no persistence call may occur, and reload must produce no transition.

[major] public/js/window-manager.js transition / restore / maximise / unmaximise / groupWindows / init; lib/workspace.js sanitizeWindow / setShelf -> Mechanism: Dock state is still overloaded and internally inconsistent. Docked → maximised → unmaximised and docked → tabbed → floating transitions retain the old non-null dock; initialization repairs only rows whose state is literally docked, so {state:'floating', dock:'left'} survives. A later minimise/restore interprets that stale edge as proof that the window should redock. The server accepts any named dock on any screen-space lifecycle state and has no per-surface edge policy. Initialization repair is also fire-and-forget: persist(model) catches its own failure and init() neither awaits nor reports durability, so a malformed row can recur indefinitely. -> Recommendation: Stop using dock simultaneously as current presentation and restore metadata. Enforce a shared canonical invariant—such as dock !== null only while state === 'docked'—and persist separate restoreState/restoreDock metadata for shelf and maximise restoration. Put policy validation and idempotent repair server-side, and make initialization await or durably queue required repairs. -> Acceptance test: Seed every inconsistent combination of lifecycle state, collapse flag, dock edge, and surface policy; include a failed first repair write and a concurrent geometry update. One initialization/reconciliation pass must converge permanently without losing geometry, and all subsequent GETs must satisfy the invariant.

[major] public/js/window-manager.js writeChain / persistStructure / persistShelfMembership; public/js/creative-desk.js persistObject / persistViewport; public/js/workspace-ui.js persist; lib/workspace.js write / setViewport / setGroups / setShelf -> Mechanism: One workspace-wide revision is incremented by unrelated last-write-wins geometry and viewport updates, while the manager’s “ALL workspace writes” serialization applies only inside that manager. CreativeDesk, WorkspaceShell, other tabs, and Partner actions use independent queues. Group and shelf requests therefore conflict with unrelated pan or panel movement. On 409, the client replays its entire stale local group/shelf collection once instead of merging server truth, so disjoint edits can be overwritten; groupsWarned and shelfWarned also serve as retry latches, meaning an earlier transport warning can suppress the next legitimate conflict retry. -> Recommendation: Separate structural revisioning from spatial/viewport revisioning and replace whole-collection writes with idempotent entity intents such as group.addMember, group.removeMember, shelf.minimise, and shelf.restore. Use independent bounded retry counters or idempotency keys and adopt canonical affected entities from every response. -> Acceptance test: Continuously pan and move legacy/world objects while two clients and a Partner action perform disjoint grouping and shelving from the same starting snapshot. No structural intent may be invalidated by unrelated geometry; all disjoint edits must survive, and a prior network failure must not disable subsequent conflict recovery.

[major] public/js/window-manager.js minimise / restore / groupWindows / joinGroup / removeFromGroup / init; lib/workspace.js setGroups / setShelf / validateWorkspace; lib/tests/workspace-routes.test.js group/shelf case -> Mechanism: Group and shelf ownership remain contradictory. minimise() removes a member locally but never calls persistStructure; its object upsert omits groupId, and setShelf changes only shelf/state, leaving the server group intact. The route test explicitly accepts a grouped member becoming shelved. setGroups permits one window in multiple group arrays; a Map silently assigns its reverse window.groupId to the last group, while validation never enforces exact bidirectional membership. joinGroup persists only the joining member’s tabbed state, not the newly grouped target’s state. Server singleton groups survive, while client initialization discards groups with fewer than two members, causing recurring reload repair. -> Recommendation: Implement one authoritative server transaction per group/shelf intent. Enforce global unique membership, exact group.windowIds ↔ window.groupId correspondence, shelf/group mutual exclusion, state compatibility for every member, and one documented singleton policy. Replace the affected client structures from the returned canonical workspace. -> Acceptance test: Property-test randomized join, regroup, minimise, restore, close, tear-out, reorder, and reload sequences. After every operation, each window must be in at most one group, reverse references must agree, no shelf member may be grouped, every active member must exist, state must match ownership, and client/server snapshots must be equivalent.

[major] public/js/window-manager.js renderFrame / transition / groupWindows / joinGroup / switchTab / applySnap / maximise -> Mechanism: A tab group is still a list of independent spatial windows rather than one frame. Members retain separate rects, z-orders, docks, restore rects, lifecycle states, and gesture locks. Switching tabs can therefore jump the frame to a member’s unrelated geometry. More severely, inactive-member hiding requires model.state === 'tabbed'; a target left floating by joinGroup, or an active member changed to docked/maximised, is no longer hidden when another tab becomes active, allowing multiple group members to render simultaneously. Different member IDs can also acquire gestures concurrently against what users perceive as one aggregate. -> Recommendation: Create a first-class group-frame record owning rect, z-order, focus, dock, maximise/restore state, and gesture ownership. Members should own content identity and tab ordering only; every spatial operation on a grouped member must route through the group frame. -> Acceptance test: Group windows with deliberately different geometry, then switch, resize, dock, maximise, restore, reload, and initiate concurrent pointers on two member tabs. The visible frame must never move merely because the active tab changed, exactly one member’s content must be visible, and group-scope gesture ownership must serialize conflicting operations.

[major] public/js/app.js init / mountBeatTrail / WorkspaceShell registrations / freeform startup -> Mechanism: Freeform mode still creates overlapping owners. The legacy BeatTrail is constructed before useFreeformDesk is evaluated; opening the registry Beats surface constructs a second trail and overwrites state.beatTrail. Destroying that registry controller sets the shared reference to null, leaving the original instance present but no longer reachable by shot-switch and Partner-refresh paths. Layers and Scenes are always registered with WorkspaceShell even though freeform startup also opens registry-owned versions, preserving duplicate controller/listener/write paths. -> Recommendation: Resolve mode and ownership before constructing any surface controller. Maintain an explicit ownership table with one live controller, one frame, one listener set, and one stable façade per logical surface; implement transfer/destroy hooks and do not register legacy owners when the registry owns the surface. -> Acceptance test: In freeform mode, instrument constructor counts, listener counts, DOM owners, and workspace writes for Layers, Scenes, and Beats. Open, close, and reopen Beats, switch shots, and trigger Partner refreshes; exactly one owner must receive every update and no panel_* writer may remain for registry-owned surfaces.

[major] public/js/window-manager.js clampRect / defaultRect / init; public/js/workspace-ui.js normalizedRect / dockRect; public/js/app.js resize handling; dev/browser-freeform-desk-journey.js Chromium launch -> Mechanism: Restored and live geometry is not responsive. clampRect constrains width/height but leaves persisted x/y untouched, and can intentionally produce a minimum-sized window larger than a small stage. The manager has no stage resize/reflow entry point, so right/bottom docks detach after viewport changes and off-screen title bars remain inaccessible until reload or storage clearing. The only native journey fixes the viewport at 1440×900. -> Recommendation: Either formally gate the desk to a supported desktop range or add responsive normalization, safe-area/chrome reservations, dock reflow, a reachable-titlebar invariant, and a “bring all windows into view” command. Reflow must operate against canonical geometry rather than destructively rewriting world placement. -> Acceptance test: Seed extreme negative and oversized positions, then test first load and continuous resize/orientation changes at 320×568, 768×1024, 1280×720, 1440×900, and ultrawide dimensions. Every open window must retain reachable controls, every dock must remain anchored or explicitly become floating, and pre/post-reload geometry must be equivalent.

[major] public/js/window-manager.js ensureFrame / focus / renderGroupTabs; public/css/freeform-desk.css resize handles/tabs/shelf -> Mechanism: Core window management remains pointer-only and lacks an accessible window model. Window sections have no region/dialog role or accessible name, logical focus() does not move DOM focus, tabs lack tablist/tab/aria-selected, and resize handles are aria-hidden with 9–15 px hit areas. There is no keyboard route for moving, resizing, docking, grouping, tearing out, minimising, maximising, or closing, and focus restoration after state changes is undefined. -> Recommendation: Add named window regions, managed DOM focus, semantic tablists, announced lifecycle changes, keyboard commands for all core operations, and larger invisible pointer targets. Define focus transfer before implementing group frames so the aggregate model has one focus owner. -> Acceptance test: Complete the normal open, focus, move, resize, group, switch, tear, minimise, restore, dock, maximise, and close journey with keyboard only and then with a screen reader. Focus must remain visible and deterministic, and every state/selection change must expose the correct role, name, selected state, and status.

[major] dev/browser-freeform-desk-journey.js windowRect and steps 18–19 -> Mechanism: Commit 9e4a981 does not compare full rectangle equality. windowRect() returns only {left, top}, so step 19 permits width and height drift while claiming that the rect survived verbatim. It also does not compare the canonical manager rect, lifecycle state, dock, or group-frame ownership. -> Recommendation: Capture {x, y, width, height} from raindeskFreeform.state(), plus the rendered DOM rectangle where projection matters, before and after reload. Compare canonical values exactly or with an explicitly justified projection tolerance and include state/dock/group identity in the witness. -> Acceptance test: Mutate restoration to alter only width or height while preserving left/top; step 19 must fail and print the complete pre/post canonical and rendered rectangles. A separate mutation changing dock or group ownership without moving the top-left corner must also fail.

[major] package.json test script; tests/frontend/freeform-window-manager.test.js close/dock/repair/gesture cases; dev/browser-freeform-desk-journey.js openPage / reload steps / console collection; FREEFORM_DESK_V2.md §7 -> Mechanism: The evidence base still cannot falsify the repaired failure classes. The default test command runs only Node tests; the native journey is separate. Close tests use immediately resolved adapters and assert a delete call rather than deferred-wire ordering and final server state. The shelf-dock test misses collapse/body visibility, the repair test omits failed persistence, and no test cancels after anchored-edge undocking. There is no mutation test. The browser journey uses /json/new after closing only the CDP WebSocket, so its “reloads” create new targets while old application pages can remain alive; each new CDP instance also resets consoleErrors, making step 25 check only the final page rather than the whole journey. Evidence is limited to one Chromium viewport. -> Recommendation: Add deterministic deferred adapters and end-to-end GET assertions for every wire race; use true Page.reload or explicitly close old targets; aggregate diagnostics across the complete run; add mutation testing; and run a viewport/browser matrix in the default verification workflow. -> Acceptance test: The suite must fail when independently removing unconditional deletion, revision adoption, restore collapse clearing, durable repair, full resize rollback, world conversion, group/shelf exclusion, or width/height comparison. An injected early-page console warning must still fail the final gate after multiple reloads.

SPECULATIVE [major] public/js/window-manager.js installTabTear / installShelfChipDrag / beginGesture; corresponding severed-buttons test -> Mechanism: The new buttons === 0 terminal closes the common re-entry path but does not prove release-outside safety. Tab and shelf sessions use document listeners without pointer capture or a session generation. If release occurs outside the browser and no zero-buttons move is delivered before a later same-ID press, the stale session can observe that new gesture’s buttons === 1 move and pointer-up, then tear or restore at unrelated coordinates. The test named for “tab tear and shelf chip” exercises only the tab path and does not inject outside release or pointer-ID reuse. -> Recommendation: Add generation-token ownership to every gesture and a site-specific capture/terminal strategy that can prove which session owns an event. A stale session must be unable to consume events from a later contact even when pointer IDs are reused. -> Acceptance test: In native browsers, release outside the window, re-enter without an intermediate move where possible, and immediately begin another gesture using a reused pointer ID for both tab and shelf sites. The old session must terminate without mutation, and mutation testing must show that removing the generation guard fails deterministically.

[major] FREEFORM_DESK_V2.md §§1–10; docs/reviews/GPT_PRO_ROUND4_VERDICT.md; current workspace/window architecture -> Mechanism: The remaining roadmap items have hard dependencies and should not be pursued as independent UI slices. Group frames built before canonical server intents would encode the current contradictory ownership model; viewport work before coordinate unification would normalize the wrong units; and further surface work before ownership retirement would add writers to an already duplicated system. -> Recommendation: Use this implementation order: (1) identity-safe lifecycle and intent-based group/shelf transactions; (2) canonical world-coordinate storage and projection; (3) bespoke ownership retirement; (4) first-class group frame; (5) viewport reflow/recovery with the multi-viewport/browser matrix; (6) keyboard/accessibility implementation against the stabilized frame model. Accessibility semantics should be designed in parallel, but final bindings should target the authoritative frame/ownership model rather than today’s temporary DOM. -> Acceptance test: Each stage must have a blocking invariant gate: no resurrection/lost structural edits after stage 1; no mixed coordinate semantics after stage 2; one owner per surface after stage 3; one geometry/lifecycle owner per group after stage 4; reachable equivalent geometry across the matrix after stage 5; and complete keyboard/screen-reader operation after stage 6.

[minor] public/js/window-manager.js CreativeSurfaces.register / get -> Mechanism: supportedStates, dockEdges, and minimumSize are now frozen, but the registry is not fully immutable. defaultPlacement retains the caller’s original mutable object, while contextualTools is a mutable shallow copy whose nested definitions are also mutable. A caller retaining the source placement object—or any consumer receiving the definition—can still change placement/tool policy after registration. -> Recommendation: Normalize, clone, and deep-freeze every declarative field at registration, or keep definitions private and expose read-only predicates/accessors rather than policy objects. -> Acceptance test: Retain references to every source object/array, retrieve the registered definition, and attempt mutation at every nesting level. Attempts must throw or have no effect, and later window placement/tools must remain identical.

[minor] public/css/freeform-desk.css .freeform-snap-zone; public/js/workspace-ui.js edgeSnap; public/js/window-manager.js snapPlace / drag preview/commit / manager-wide overlay state -> Mechanism: Snap feedback still does not predict settlement. Left/right/top/bottom washes cover 32% of the desk, while docking activates only when a window edge is within 18 px. Preview is computed from the most recent move’s modifier state, but release recomputes from pointer-up, so changing Alt without moving can settle differently from the displayed candidate. The manager also allows gestures on different windows while maintaining only one global preview and zone layer; one gesture can overwrite or clear another’s feedback. -> Recommendation: Make the visible zone the actual hit-test contract or reduce it to the real activation band; settle the exact candidate last presented; and key overlays by gesture session or enforce one desk-level docking gesture. -> Acceptance test: Sample releases throughout every visible zone, change Alt without moving, and drag two windows toward opposite edges with separate pointers. Visible active/inactive state must exactly predict commit, and one gesture’s terminal must not erase feedback owned by another.

[minor] public/js/window-manager.js ensureFrame / open / init; dev/browser-freeform-desk-journey.js step 9 -> Mechanism: Rename and restored-title evidence is synthetic. Double-click enables editing only when model.onRename exists, but production default opens do not provide it. The journey bypasses that gate by directly changing textContent and dispatching blur. Titles are not persisted; initialization assigns win.windowId, and the restored controller’s setTitle callback is a no-op, so a reload can replace “Scenes” with window_scenes and cannot restore a custom title. -> Recommendation: Either remove rename from the contract or implement a real title source: persist a bounded title or derive the default from the registered surface while allowing a separately persisted override. Exercise the actual double-click/keyboard path. -> Acceptance test: Rename a production surface through native input, reload, and verify the documented title policy. A mutation disabling the editing entry path must fail the journey; restored default titles must never degrade to internal IDs.

[minor] docs/reviews/GPT_PRO_ROUND4_VERDICT.md status against d4589c0 / 9e4a981 -> Mechanism: The Round-4 verdict now contains stale mechanism descriptions for the persisted-close gate, always-floating shelf restore, resize capture ordering, missing tab/shelf severed checks, mutable supportedStates, and the absence of any positional reload comparison. Those narrow mechanisms have changed, but the broader lifecycle identity, structural concurrency, coordinate, group-frame, ownership, accessibility, viewport, and evidence conclusions are not disproved and should not be marked resolved. -> Recommendation: Append a finding-by-finding status ledger—fixed, partial, open, or superseded mechanism—with the exact discriminating test for each. Record the collapsed dock restore, full rollback, failure classification, and full-rectangle evidence residuals under the corresponding partial findings. -> Acceptance test: Every Round-4 finding must map to a commit and a test that distinguishes the old and current mechanism; no superseded description may be presented as current, and no partial closure may be labelled complete while its residual acceptance case remains red.

TOP-3 next work items

Identity-safe, intent-based lifecycle/group/shelf transactions — very high impact / medium-to-high effort.
Acceptance criterion: Immediate close, lost responses, repeated conflicts, stale clients, simultaneous tabs, Partner writes, group changes, and shelving complete without resurrection, unrelated conflicts, invalid hybrid ownership, or lost disjoint edits; every returned workspace satisfies exact lifecycle, group, shelf, dock, and reverse-reference invariants.

Canonical world-coordinate unification — highest architectural impact / high effort.
Acceptance criterion: Every creative surface persists one canonical world rect and retains its relationship to artwork through pan, zoom, viewport resize, grouping, docking, minimising, maximising, restoration, and reload; explicitly declared application chrome is the only viewport-fixed exception.

Retire bespoke surface ownership before implementing the group-frame model — high impact / medium effort.
Acceptance criterion: Freeform mode has exactly one controller, one DOM owner, one listener set, and one persistence writer for each logical surface; close/reopen, shot switching, and Partner refreshes continue through a stable façade with no legacy panel_* writes or orphaned controllers.
