[critical] public/js/freeform-surfaces.js: installSurfaces() + lib/workspace.js: WINDOW_TYPES, ENTITY_REF_RE, sanitizeWindow() + public/js/window-manager.js: persist(), surfaceIdForEntityType(), init() -> Mechanism: four shipped surfaces use entity types the server does not recognize: take_stack, character_registry, notes_panel, and partner_proposals. sanitizeWindow() silently converts each initial upsert to generic_panel; the manager ignores the canonical object returned by the server and continues believing the original type is stored. On reload, surfaceIdForEntityType() requires an exact registered entity-type match, so these windows are skipped and disappear. The declared characters:, notes:, and proposals: entity-reference prefixes are also rejected by ENTITY_REF_RE; only takes: is admitted. The browser journey masks this for Notes by explicitly calling open('notes') after reload rather than proving restoration. -> Concrete recommendation: define one canonical surface-schema manifest consumed by both browser and server; add the four exact entity types and all declared prefixes, reject rather than silently coerce unknown types, make the manager adopt canonical response fields, and migrate existing window_takes, window_characters, window_notes, and window_proposals rows that were stored as generic_panel. -> Acceptance test: open all seven real surfaces with typed entity references, move and state-transition each, inspect GET /api/workspace, reload without calling open(), and prove every surface restores with its exact type, entityRef, state, and rectangle; seed the four legacy generic_panel rows and prove the migration restores them to the intended surface.

[critical] public/js/app.js: init() freeform-manager construction + public/js/window-manager.js: defaultRect(), renderFrame(), installDrag(), installResize(), snapPlace() + public/js/creative-desk.js: worldScale(), worldToScreen(), screenToWorld(), renderObject() + lib/workspace.js: SCREEN_TYPES/defaultSpaceForType() -> Mechanism: the implementation still has two coordinate authorities. CreativeDesk objects are stored in world units and reprojected whenever the desk pans or zooms, while freeform windows are mounted directly under #stage, rendered in raw CSS pixels, and moved/resized from unscaled clientX/clientY deltas. Panning or zooming therefore moves the artwork and creative sheets but leaves registry windows fixed to the screen; dragging at different zoom levels persists different logical distances. The server further classifies Scenes, Layers, and Beats as screen-space, while unrecognized surface types become screen-space generic_panel. This contradicts the round-3 contract that all creative windows share the desk’s world-space coordinate authority. -> Concrete recommendation: introduce an explicit coordinate adapter into WindowManager—clientToWorld, worldToClient, and worldScale—and persist floating/group geometry in world units. Define docking as an explicit state conversion: either world-edge docking, or viewport-edge docking with a preserved restoreWorldRect; do not store raw screen rectangles as if they were world geometry. Retire the separate CreativeDesk frame/gesture authority once the adapter is proven. -> Acceptance test: at zoom factors 0.5, 1, and 2, pan the desk, drag and resize every surface by the same on-screen distance, and prove the correct world delta is stored; reload at a different viewport size and zoom and prove floating windows retain their relationship to the artwork while docked windows retain their documented edge behavior.

[major] public/js/window-manager.js: open(), persist() + public/js/api.js: upsertWorkspaceObject(), deleteWorkspaceWindow() + server-core.js: /api/workspace/object and /api/workspace/window/delete + lib/workspace.js: upsertWindow(), upsertObject(), deleteWindow() -> Mechanism: structural creation is routed through the same ungated last-write-wins upsert used for drag geometry. An update for a missing ID silently recreates the window. Consequently, after one browser deletes a window, a stale browser—or an already queued drag save—can send /api/workspace/object and resurrect it without observing a revision conflict. This defeats the stated revision gate for structural create/delete. -> Concrete recommendation: separate revision-gated create from existence-preserving spatial update. A spatial update must return 404 or 409 when its window no longer exists or its generation/tombstone has changed; creation must carry baseRevision. Include a durable generation or tombstone token in window mutations so stale clients cannot recreate deleted identity. -> Acceptance test: open the same workspace in two browser contexts, delete a window in context A, then release a drag and flush a previously queued save from context B; the stale writes must fail and the window must remain absent after reload.

[major] public/js/window-manager.js: close() -> Mechanism: closing a window destroys only the local controller and DOM frame. It never calls api.deleteWorkspaceWindow(). persistStructure() can update group membership, but the window row itself—and, for a minimised window, its shelf membership—remains on the server. Known surface types therefore reappear on reload. Any queued persist(model) closure also retains the removed model and can upsert it after the local close. -> Concrete recommendation: make close a serialized lifecycle transaction: terminate and invalidate active gestures, invalidate or await older per-window saves, issue the revision-gated server deletion, adopt the returned revision, then destroy local state. The server deletion should remain authoritative for group and shelf cleanup. Use a per-window generation token so pre-close promises cannot write afterward. -> Acceptance test: close floating, grouped, docked, and minimised windows and immediately reload; each must remain absent from GET /api/workspace and the DOM. Repeat while an upsert is deliberately held in flight and prove its late completion cannot recreate the window.

[major] public/js/window-manager.js: persist(), snapPlace(), applySnap(), init() + lib/workspace.js: sanitizeWindow(), setShelf() -> Mechanism: docking is not durably represented. applySnap() changes only model.rect and model.state; the model has no dock edge, and persist() omits the server’s dock field. init() then explicitly converts every persisted docked state back to floating. Minimise/restore cannot recover the edge either because setShelf() only restores docked when a stored dock value exists. The visible result is that a docked arrangement always un-docks after reload or a shelf round trip. -> Concrete recommendation: add model.dock, set and clear it during snap/refloat, persist it, and preserve a pre-dock restoreRect in the correct coordinate space. On initialization and viewport change, derive current dock geometry from the edge plus current metrics rather than trusting an old pixel rectangle. -> Acceptance test: dock a surface to each of the four edges, reload while it is still docked, minimise and restore it, and reopen at a different viewport size; the same edge must remain authoritative and the window must remain fully reachable.

[major] public/js/window-manager.js: installResize(), renderFrame() + public/js/workspace-ui.js: normalizedRect(), dockRect() -> Mechanism: a docked window can be resized away from its edge while its state remains docked. For example, resizing the west edge of a left-docked window changes x; resizing the north edge of a top-docked window changes y. Resize completion neither reapplies dockRect() nor transitions to floating, and it does not normalize the result to the viewport. A later viewport shrink also has no manager reflow path, leaving stale docked rectangles detached or partially off-screen. -> Concrete recommendation: make dock geometry an invariant. Either restrict docked resizing to dimensions that preserve the anchored edge, or transition to floating as soon as the anchored edge is pulled away. Add a manager reflow() driven by ResizeObserver that recomputes docked geometry and preserves a recoverable title-bar region for floating windows. -> Acceptance test: for every edge, resize from all eight grips and resize the browser while docked; the window must either remain exactly attached to its recorded edge or explicitly become floating, with state and persisted metadata agreeing.

[major] public/js/window-manager.js: CreativeSurfaces.register() + public/js/freeform-surfaces.js: all seven registrations + public/js/workspace-ui.js: dockRect() -> Mechanism: docking capability is enabled by default and every real surface explicitly permits all four edges, but there is no per-surface edge policy or adaptive dock layout. dockRect() merely moves the current arbitrary rectangle to an edge; it does not create a side rail or horizontal strip. Vertical content such as Scenes, Layers, Beats, Characters, Notes, and Proposals can be placed against top/bottom without a horizontal renderer, while the compact Takes controls can become an isolated edge-touching card rather than a useful dock. This is the primary dock-capable-by-default rollout surprise: the state exists uniformly, but the content and sizing contracts do not. -> Concrete recommendation: replace the Boolean docked capability with a dockPolicy containing allowed edges, dock-size constraints, and optional edge-specific layouts. Initially permit left/right for the vertical list/editor surfaces; disable top/bottom until horizontal variants exist; opt Takes out entirely until it has a real strip layout. Do not make future surfaces dock-capable unless they explicitly declare a policy. -> Acceptance test: run a seven-surfaces-by-four-edges matrix at normal and minimum supported viewport sizes. Unsupported edges must show no zone and never dock; supported edges must preserve all controls, scrollability, minimum content area, and an unobscured central art workspace.

[major] public/js/window-manager.js: installDrag() -> Mechanism: pointercancel is wired to the normal up commit handler. An operating-system cancellation, browser gesture takeover, device interruption, or DOM disruption after movement therefore performs shelf/group hit testing, can edge-snap, can transition dock state, and persists the cancelled rectangle. A cancellation event with unusual coordinates can even commit a different edge or drop target from the last visible position. -> Concrete recommendation: give drag a dedicated cancel terminal that restores the full pre-gesture snapshot—rectangle, state, dock metadata, group/shelf membership, and z-order as applicable—clears overlays, releases capture, detaches listeners, and performs no persistence. -> Acceptance test: cancel a moved pointer while over each edge, the shelf, another window, and empty space; model state, server state, group/shelf membership, and rectangle must exactly match the pre-gesture snapshot, with zero mutation API calls.

[major] public/js/window-manager.js: installDrag(), installResize(), installTabTear(), installShelfChipDrag() -> Mechanism: terminal pointer handling is incomplete and inconsistent. Drag’s buttons === 0 path detaches listeners but leaves the already mutated in-memory rectangle neither rolled back nor persisted; Escape does not release pointer capture; no path handles lostpointercapture, window blur, or document visibility loss. Resize lacks a severed-buttons guard. Tab and shelf drags have no pointercancel handler or capture, so an outside release can leave document listeners alive until a later reused pointer ID finishes an unrelated gesture. -> Concrete recommendation: centralize all pointer interactions in one GestureSession abstraction with a pre-gesture snapshot, one owner, and explicit commit, cancel, and severed terminals. Every terminal must detach all listeners, release capture if held, clear overlays, and make an explicit rollback-versus-commit decision; handle pointercancel, lostpointercapture, blur, visibility change, and element destruction. -> Acceptance test: exercise release outside the viewport, browser blur, lostpointercapture, buttons === 0, visibility change, and close-during-gesture for drag, resize, tab reorder/tear, and shelf tear-out; no listeners or overlays may remain, and model and server state must be identical and deterministic.

[major] public/js/window-manager.js: installDrag(), installResize() -> Mechanism: pointer-ID filtering protects one gesture from foreign events, but there is no per-window active-gesture lock. Two touch contacts can independently start two header drags, two grips, or a drag plus resize on the same mutable model. Both closures then alternately overwrite rect from different start snapshots and enqueue competing saves, producing order-dependent geometry. -> Concrete recommendation: maintain one active gesture token per window and reject every later pointerdown until that token terminates. Prefer the primary pointer when no owner exists, but do not rely on isPrimary alone; close and controller destruction must revoke the token. -> Acceptance test: start simultaneous contacts on two grips and on a header plus grip, move them in alternating order, and release in both orders; exactly one gesture must own the window, exactly one final save must occur, and repeated runs must produce the same rectangle.

[major] public/js/window-manager.js: persistStructure(), persistShelfMembership() + server-core.js: /api/workspace/groups and /api/workspace/shelf + lib/workspace.js: setGroups(), setShelf() -> Mechanism: conflict handling adopts the server revision and blindly retries the same whole-collection replacement. If two tabs independently create group G1 and G2, the loser’s retry replaces the server group array with only its local collection and deletes the winner’s group. Likewise, two tabs minimising different windows can cause the retrying shelf list to remove the first window. groupsWarned and shelfWarned also double as warning and retry latches, so an earlier transport failure can suppress a later conflict retry. -> Concrete recommendation: use operation-based structural mutations—add/remove group member, create/dissolve group, reorder member, minimise/restore one window—or fetch and semantically merge by stable IDs before retrying. Never retry a stale whole-array snapshot unchanged. Separate retry count from user-warning state. -> Acceptance test: coordinate two browser contexts so they land disjoint group changes and disjoint shelf additions from the same revision; all nonconflicting changes must survive. Conflicting edits to the same member must return a visible conflict or follow a documented deterministic rule, never silently overwrite.

[major] public/js/window-manager.js: minimise(), removeFromGroup(), persist(), persistShelfMembership() + lib/workspace.js: setGroups(), setShelf() -> Mechanism: minimising a grouped window removes it from the local group but never calls persistStructure(), and the ordinary window upsert omits groupId. The server therefore keeps the window in its old group while also adding it to the shelf and setting it minimised. On reload, the client can reconstruct a group containing a shelf-backed member; restoring that member can leave it floating while still carrying group membership. The multi-request sequence can also land partially. -> Concrete recommendation: expose one revision-gated server transaction such as minimiseWindow(windowId) that removes group membership, updates or dissolves the group, changes the window state, and updates shelf membership atomically. Apply the returned canonical workspace to the client. -> Acceptance test: minimise active and inactive members from two-, three-, and singleton-policy groups; server state must show the member in exactly one structural location, reload must be identical to pre-reload state, and injected failure at any internal step must leave either the complete old state or complete new state—never a hybrid.

[major] public/js/window-manager.js: groupWindows(), renderFrame(), transition(), restore(), maximise(), unmaximise(), applySnap(), switchTab() -> Mechanism: a group has neither authoritative frame geometry nor authoritative frame state. Each member retains its own rectangle, so switching tabs can move and resize the visible outer frame. Resizing one active tab changes only that member. More severely, restore, maximise/unmaximise, or docking can change a member from tabbed to floating, maximised, or docked while retaining groupId; renderFrame() hides inactive members only when their state is exactly tabbed. After a later tab switch, multiple frames from the same group can remain visible. -> Concrete recommendation: make rectangle and spatial state group-owned. Keep all members in a content-membership state and apply move/resize/maximise/dock to the group frame, or explicitly ungroup a member before any independent spatial transition. Enforce the invariant that a member cannot retain groupId in an incompatible per-window state. -> Acceptance test: group windows with deliberately different rectangles, switch repeatedly, resize, maximise/unmaximise, dock, restore an inactive member, and reload; the outer frame must never jump unexpectedly, exactly one member must be visible, and group state must remain canonical.

[major] public/js/window-manager.js: groupWindows(), removeFromGroup(), init() + lib/workspace.js: setGroups(), validateWorkspace() -> Mechanism: group graph invariants differ across paths. groupWindows() does not remove requested windows from existing groups and does not deduplicate IDs. The server rejects duplicates within one group but permits the same window to appear in multiple different groups; its membership map simply gives the last group precedence while retaining all conflicting arrays. Separately, removeFromGroup() intentionally preserves a one-member group live, whereas init() discards every persisted group with fewer than two live members and silently refloats its member. Re-grouping can therefore create contradictory tab strips, and a singleton changes semantics merely by reloading. -> Concrete recommendation: normalize members out of previous groups before forming a new one, reject duplicate input locally, and make server validation enforce at most one group per window plus exact agreement between group.windowIds and window.groupId. Choose one singleton policy and enforce it identically during mutation, validation, and restoration; dissolving immediately below two members is the simpler UI invariant. -> Acceptance test: regroup members drawn from two existing groups, submit a malicious cross-group duplicate payload, remove one member from a two-member group, and reload; the malformed payload must return 400, no old group may retain moved members, and singleton behavior must be unchanged by reload.

[major] public/js/app.js: init() + public/js/creative-desk.js: ensureElement(), installDrag(), installResize() -> Mechanism: registry ownership is still additive rather than exclusive. Freeform mode continues to register and initialize legacy Layers and Scenes through WorkspaceShell, so persisted panel_layers or panel_scenes visibility can display old and registry versions simultaneously. The legacy BeatTrail is instantiated before the flag branch; mounting window_beats creates a second BeatTrail and overwrites state.beatTrail without destroying the first instance. CreativeDesk sheets and Reference Board still own separate frames, drag/resize logic, tabs, and persistence. This leaves duplicate renderers, listeners, identities, and spatial authorities active in one mode. -> Concrete recommendation: retire ownership in this order: first gate legacy Layers/Scenes registration and rendering off in freeform mode; second instantiate exactly one BeatTrail behind a registry adapter; third migrate CreativeDesk/Reference Board frame and gesture ownership onto the shared coordinate-aware manager; last, adapt Partner/drawer/surface-handoff routes while preserving the intentional mobile fallback. Encode the ownership table in assertions rather than comments. -> Acceptance test: under ?freeform=1, prove there is exactly one controller and one visible DOM owner per semantic surface, no panel_layers, panel_scenes, or panel_beats mutations are emitted, only one BeatTrail listener/controller instance exists across open/close cycles, and every spatial mutation goes through the registry manager.

[major] public/js/window-manager.js: clampRect(), defaultRect(), installDrag(), init() + public/js/app.js: useFreeformDesk + public/css/freeform-desk.css + public/js/workspace-ui.js: normalizedRect() -> Mechanism: recoverability is not enforced after first placement. clampRect() constrains only width and height, ordinary dragging can place the entire title bar outside the root, persisted restoration does not call normalizedRect(), and no viewport-resize reflow exists. The freeform flag also ignores the legacy shell’s min-width: 900px desktop boundary, while the CSS has no small-screen mode and uses 9–15 px pointer grips. An Alt-drag, monitor change, or opening the flag on a sub-900 viewport can therefore leave windows unreachable beneath overlapping mobile/legacy chrome. -> Concrete recommendation: require a minimum visible title-bar region after every committed move, restore, and viewport change; add a reveal-active command and ResizeObserver reflow. Until a responsive window mode exists, gate freeform to the same desktop media query and fall back to the legacy mobile drawer. A later mobile design should use full-screen/stacked surfaces and shelf navigation rather than desktop floating geometry. -> Acceptance test: drag windows beyond every edge with and without Alt, reload on a smaller monitor, and test 899×700, 768×1024, 390×844, and landscape equivalents; every window must remain recoverable and all controls must remain reachable without duplicate overlays.

[major] public/js/window-manager.js: ensureFrame(), renderFrame(), focus(), installResize(), renderGroupTabs() + public/css/freeform-desk.css: resize-handle and tab rules -> Mechanism: the core window lifecycle is pointer-only and semantically incomplete. Frames are unlabelled <section> elements with no keyboard focus target, manager focus does not move DOM focus, resize handles are aria-hidden, and there are no keyboard commands for move, resize, dock, maximise, minimise, or close. Group tabs lack tablist/tab roles, aria-selected, and controlled-panel relationships. The visually tiny resize strips also provide no larger touch hit surface. Keyboard and assistive-technology users cannot operate or understand the desk state. -> Concrete recommendation: give each window a labelled region/dialog contract, establish deterministic DOM-focus entry and restoration, implement a keyboard window menu plus move/resize commands, expose tab semantics and announcements, and use touch-sized invisible hit regions around the visual resize strips. -> Acceptance test: complete open, focus, tab switch, move, resize, dock, minimise, restore, maximise, and close using only a keyboard; a screen reader must announce title and current state, focus must return predictably on close, and an automated accessibility scan must report no critical window/tab violations.

[major] dev/browser-freeform-desk-journey.js: fixed Chromium launch and steps 8, 18–25 + tests/frontend/freeform-window-manager.test.js: fake pointer-capture DOM -> Mechanism: current green evidence cannot detect the principal failures above. The only native journey runs a mouse at 1440×900; it docks and immediately refloats before reload, labels step 19 as geometry restoration without comparing rectangle values, and explicitly reopens Notes after reload. It does not prove restoration of all seven surface identities, real pointer cancellation/lost capture, touch or pen behavior, simultaneous pointers, docked resizing, modifier changes, responsive layouts, world zoom, keyboard use, reduced motion, or two-tab structural conflicts. The fake DOM’s no-op pointer capture cannot model browser capture loss. -> Concrete recommendation: add a real-browser matrix covering pointer types, DPR/zoom, viewport sizes, reduced motion, all seven surfaces, and two synchronized browser contexts. Assert both observable DOM and canonical GET /api/workspace state after every transition; do not repair missing windows by calling open() inside restoration tests. -> Acceptance test: CI must run at least desktop normal/small and mobile-fallback viewports, DPR 1/2, zoom 0.5/1/2, mouse/touch/pen cancellation cases, all four dock edges, every supported surface state, and deterministic two-context conflict scenarios, with exact pre/post/reload state comparisons.

[minor] public/js/window-manager.js: ensureFrame() title dblclick/blur handlers, persist(), init() + dev/browser-freeform-desk-journey.js: step 9 -> Mechanism: the documented double-click rename path is unavailable for ordinary registry surfaces because the handler returns when model.onRename is absent, and the seven standard opens do not supply one. The journey bypasses this by directly editing textContent and dispatching blur. Even when the model title changes, title is not part of the workspace payload and initialization resets it to windowId. -> Concrete recommendation: either make manager-local renaming universally available and persist a bounded title field, or remove the affordance and contract. Treat onRename as an optional notification, not permission to edit. -> Acceptance test: perform a real double-click, type and confirm a title using mouse and keyboard, reload, and prove the title remains; the test must not mutate DOM text directly.

[minor] public/js/window-manager.js: installTabTear(), tearOut() versus installShelfChipDrag() -> Mechanism: tab tear-out writes viewport clientX/clientY directly into a rectangle rendered relative to the manager root. Shelf tear-out correctly subtracts root.getBoundingClientRect(). When #stage has a nonzero top or left offset, a tab is therefore dropped displaced by that offset; world zoom would compound the error. -> Concrete recommendation: route every pointer location through one clientToManagerPoint() or world-coordinate adapter before writing geometry. -> Acceptance test: mount the manager root at a known nonzero offset, tear a tab out at a measured pointer position, and assert the resulting frame origin matches the intended root-local/world position within one CSS pixel.

[minor] public/js/window-manager.js: bringToFront(), focus(), persist() + lib/workspace.js: activeWindowId -> Mechanism: click-only focus and z-order changes are never persisted, and manager focus does not update the server’s activeWindowId. The arrangement can therefore reload with an older stacking order and no active window unless a later unrelated spatial mutation happens to persist the raised zIndex. -> Concrete recommendation: debounce a canonical focus/z-order write or add a lightweight focus endpoint, normalize z-indices periodically, and focus the next highest visible window after close/minimise. -> Acceptance test: change stacking using clicks only, reload without moving anything, and prove the active window and visual order are preserved and DOM focus lands on the same logical surface.

[minor] public/css/freeform-desk.css: .freeform-snap-zone + public/js/workspace-ui.js: edgeSnap() + public/js/window-manager.js: installDrag() move/up -> Mechanism: the visible edge “drop zones” occupy 32% of the desk, but the actual snap hit target is only 18 px from the normalized edge. Releasing over most of the shaded region does nothing. Preview and commit also independently sample altKey, so pressing or releasing Alt between the final move and pointerup can make the committed result disagree with the last preview. -> Concrete recommendation: make the visible geometry and actual hit geometry identical, and keep a single current snap candidate in the gesture session that both preview and commit consume. Modifier changes should synchronously update that candidate before release. -> Acceptance test: sweep release points across the exact rendered zone boundary and toggle Alt between move and release; every visibly active point must dock, every inactive point must not, and the final result must always match the last rendered preview.

[minor] public/js/window-manager.js: init() -> Mechanism: restored state is checked only against the global SUPPORTED_STATES, not the target surface’s supportedStates. If dock opt-outs or other per-surface restrictions are introduced, old persisted maximised, minimised, or future restored docked rows can still enter states the registry now declares unsupported. -> Concrete recommendation: normalize every restored state through the surface definition and an explicit migration policy, recording a repair when persisted state is downgraded. -> Acceptance test: seed each surface with every globally valid but locally unsupported state, initialize, and prove the manager chooses the documented safe state, repairs the server record, and never renders an unsupported transition.

[minor] public/js/window-manager.js: persist(), persistStructure(), persistShelfMembership(), refreshAll() + public/js/app.js: outer freeform try/catch and init().catch() -> Mechanism: persistence and surface failures are effectively invisible to the artist. The “visible warning” in persist() is only console.warn; boot and initialization failures are swallowed; controller render exceptions are discarded. A surface can stop updating or an arrangement can remain unsaved while the UI continues to look successful. -> Concrete recommendation: expose a bounded per-window unsynced/error indicator with retry, emit structured diagnostics, and show an explicit fallback status when freeform initialization fails. Continue isolating damaged surfaces, but do not suppress the evidence. -> Acceptance test: inject object, group, and shelf 500 responses plus a controller render exception; the relevant window must show one non-spamming failure state, retry must clear it after recovery, and the rest of the desk must remain operable.

TOP-3 next work items ranked by impact/effort

Canonical surface persistence and durable lifecycle — very high impact / low-to-medium effort. Unify the type/entity-reference schema, migrate the four coerced surface rows, persist dock edge metadata, distinguish create/update/delete, and make close use the authoritative delete transaction. Acceptance criterion: all seven surfaces survive state/geometry reload without explicit reopen; close remains closed under a stale in-flight writer; every server row has the exact canonical type and edge metadata.

One gesture-and-structure transaction kernel — very high impact / medium effort. Centralize gesture ownership and terminal handling, then route group/shelf/state changes through atomic operation-based APIs with semantic conflict resolution. Acceptance criterion: the cancellation/lost-capture/multi-pointer matrix produces deterministic zero-leak results, grouped minimise/restore is atomic, and two browser contexts retain all nonconflicting group and shelf changes.

Unify world coordinates and narrow the docking rollout — existential impact / high effort. Give the manager the CreativeDesk world transform, introduce group-owned frame geometry, add viewport reflow, and replace universal four-edge capability with explicit per-surface dock policies and a desktop/mobile boundary. Acceptance criterion: floating and grouped windows remain spatially stable through pan, zoom, reload, and viewport changes; only declared surface-edge combinations dock; all supported combinations pass the content, accessibility, and small-screen browser matrix.
